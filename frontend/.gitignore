# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-admin
dist-widget
dist-ssr
*.local

# Generated admin-console env (SPEC-SEPARATE-ADMIN-APP.md) — deployment URLs, not
# committed. `.env` is ignored via the root .gitignore; mirror that for .env.admin.
.env.admin

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
