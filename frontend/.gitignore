# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-widget
dist-ssr
*.local

# Per-package generated envs (SPEC-SEPARATE-ADMIN-APP.md): packages/{chat,admin}/.env
# hold deployment URLs, not committed. `.env` is ignored at any depth via the
# root .gitignore; listed here for clarity.
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
